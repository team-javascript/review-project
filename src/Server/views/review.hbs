<main>
    <div class="container">
        <ul class="review-cards">
            {{#each review}}
            <li class="review-card">
                <figure class="card"><img src="{{imageUrl}}" alt="{{title}}"></figure>
                <a class="review-card__link" href="/review-collection/{{id}}">{{title}}</a>
            </li>
            {{/each}}
        </ul>
    </div>
    <a href="/">Back to Home</a>
</main>